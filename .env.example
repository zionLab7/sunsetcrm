# ============================================
# Sunset CRM - Variáveis de Ambiente
# ============================================

# Database (PostgreSQL)
DATABASE_URL="postgresql://sunset_user:SENHA_FORTE_AQUI@db:5432/sunset_db?schema=public"

# NextAuth
NEXTAUTH_URL="https://crm.seudominio.com"
NEXTAUTH_SECRET="GERAR_COM_openssl_rand_-base64_32"

# Google Gemini AI → configurar via Configurações → Integrações no app

# Seed (apenas na primeira execução, depois remover)
# RUN_SEED=true
